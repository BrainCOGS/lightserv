version: "3.4"

services:
  # testredis:
  #   image: redis
  #   expose:
  #     - 6379
  #   restart: always
  # confproxy:
  #   env_file: .dockerenv
  #   image: jupyterhub/configurable-http-proxy:3.1.1
  #   # expose the proxy to the world
  #   ports:
  #     - "8002:8000"
  #     - "8003:8001"
  #   command: ['configurable-http-proxy',
  #             '--auto-rewrite',
  #             '--no-include-prefix',
  #             '--no-prepend-path',
  #             '--log-level', 'debug',
  #             '--api-ip', '0.0.0.0']
  testflask:
    env_file: .testdockerenv
    image: flaskcelery:test
    command: "pytest -vs lightserv/tests/test_clearing.py::test_clearing_entry_experimental_rat_nonadmin"
    volumes:
      - .:/app # so changes made to the app are propagated to the container without having to rebuild
      - /jukebox/LightSheetData:/jukebox/LightSheetData
      - /jukebox/LightSheetTransfer:/jukebox/LightSheetTransfer
      - ./lib:/opt/libraries # for progproxy api
    ports:
      - '5010:5000'
  coverage:
    env_file: .testdockerenv
    image: flaskcelery-coverage:test
    command: "pytest --cov=lightserv/clearing --cov-report html -vs lightserv/tests/test_clearing.py"
    volumes:
      - .:/app # so changes made to the app are propagated to the container without having to rebuild
      - /jukebox/LightSheetData:/jukebox/LightSheetData
      - /jukebox/LightSheetTransfer:/jukebox/LightSheetTransfer
      - ./lib:/opt/libraries # for progproxy api
    ports:
      - '5010:5000'
networks:
  default:
    external:
      name: lightserv-test